- No need to `cd` to the project directory, you are already there.
- Use `pdm run` before running any python commands to ensure the correct environment is used.
- Void functions don't have to be annotated with `-> None`, unless abstract.

# About the language
When you need to find out a fact about language syntax or behavior, the best
places to look are (in descending priority):
- `tests/flows/*.my`
- `README.md`
NEVER LOOK INSIDE `brainstorm/` TO INFORM YOURSELF ABOUT THE CURRENT FEATURES OF
THE LANGUAGE.
