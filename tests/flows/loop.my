x = 3

loop (
    while $x
    x = $x-1
    echo "x =" $x
)

echo "after 1st loop $x is" $x

loop (
    x = $x + 1
    echo "x =" $x
    while $x < 1
)

echo "after 2nd loop $x is" $x

loop (
    x = $x + 1
    if $x == 2 (
        continue,;  # TODO Fix StatementList parsing
    )
    if $x == 4 (
        break,;  # TODO Fix StatementList parsing
    )
    echo "x =" $x
)

echo "after 3rd loop $x is" $x
